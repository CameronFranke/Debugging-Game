{"program1":
    [
      {"code": "#include <iostream>"},
      {"code": "#include <string>"},
      {"code": "using namespace std;"},
      {"code": "int main() {{"},
      {"code": "string symptoms[] = {{\"cough\", \"headache\", \"running nose\", \"photophobia\", \"muscle ache\"}",
              "fixes":  ["string symptoms[] = {{cough, headache, running nose, photphobia, muscle ache}",
                        "string symptoms[] = {{\"cough\", \"headache\", \"running nose\", \"photphobia\", \"muscle ache\"}",
                        "string symptoms = {{\"cough\", \"headache\", \"running nose\", \"photphobia\", \"muscle ache\"};"]},
      {"code": "int numSymp = sizeof(symptoms)/sizeof(char);"},
      {"code": "for(int i=0; i<numSymp; i++) {{"},
      {"code": "cout << i+1 << \": \" << symptoms[i] << \" \";"},
      {"code": "}}"},
      {"code": "cout << endl << \"Do you have any of these symptoms? << endl;"},
      {"code": "cout << \"If so, enter the symptom number one at a time followed by ENTER\" << endl;"},
      {"code": "cout << \"Enter -1 to stop\" << endl;",
        "fixes": ["cout << endl << \"Do you have any of these symptoms?\"<< endl;",
                "count << \"If so, enter the symptom number one at a time followed by ENTER\" << endl;",
                "cout << \"Enter -1 to stop\" < endl;"]},
      {"code": "int patientSymptoms[numSymp] = {{ -1 };"},
      {"code": "int count = 0, input = 0;"},
      {"code": "while(count < numSymp || input >= 0) {{"},
      {"code": "count << \":: \";"},
      {"code": "cin >> input;"},
      {"code": "patientSymptoms[count] = input;"},
      {"code": "}",
        "fixes": ["Add: count++",
                  "Add: count+1;",
                  "Add: count += 1;"]},
      {"code": "bool result;"},
      {"code": "int num = 0;",
        "fixes": ["bool result = False;",
                  "int num = 1;",
                  "bool result = True"]},
      {"code": "for (int i=0; i<numSymp; i++) {{"},
      {"code": "if (patientSymptoms[i] > 0) {{"},
      {"code": "num+1;"},
      {"code": "if(symptoms[patientSymptoms[i] == \"photophopia\") {{"},
      {"code": "result = true;"},
      {"code": "}"},
      {"code": "}"},
      {"code": "if(num > 3 && result) {{"},
      {"code": "cout << \"You will turn into a walker...\" << endl;"},
      {"code": "return 0;"},
      {"code": "}"},
      {"code": ""},
      {"code": "cout << \"You are not infected\" << endl;"},
      {"code": "return 0;"}
    ]
  }
