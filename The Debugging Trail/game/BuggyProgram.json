{"program1":
    [
      {"code": "#include <iostream>\n#include <string>\nusing namespace std;\nint main() {{",
        "fixes": ["None"]},
      {"code": "string symptoms[] = {{\"cough\", \"headache\", \"running nose\", \"photphobia\", \"muscle ache\"}",
        "fixes": ["string symptoms[] = {{cough, headache, running nose, photphobia, muscle ache}",
                  "string symptoms[] = {{\"cough\", \"headache\", \"running nose\", \"photphobia\", \"muscle ache\"};",
                  "string symptoms = {{\"cough\", \"headache\", \"running nose\", \"photphobia\", \"muscle ache\"}"]},
      {"code": "int numSymp = sizeof(symptoms)/sizeof(char);",
        "fixes": ["None"]},
      {"code": "for(int i=0; i<numSymp; i++) {{\n    cout << i+1 << \": \" << symptoms[[i]] << \" \";   \n}",
        "fixes": ["None"]},
      {"code": "cout << endl << \"Do you have any of these symptoms? << endl;\ncout << \"If so, enter the symptom number one at a time followed by ENTER\" << endl;\ncout << \"Enter -1 to stop\" << endl;",
        "fixes": ["None"]},
      {"code": "int patientSymptoms[numSymp] = {{ -1 };",
        "fixes": ["None"]},
      {"code": "int count = 0, input = 0;",
        "fixes": ["None"]},
      {"code": "while(count < numSymp || input >= 0) {{\n    count << \":: \";\n    cin >> input;\n    patientSymptoms[count] = input;\n}",
        "fixes": ["None"]},
      {"code": "bool result;\nint num = 0;",
        "fixes": ["None"]},
      {"code": "for (int i=0; i<numSymp; i++) {{\n    if (patientSymptoms[i] > 0) {{\n        num+1;\n        if(symptoms[patientSymptoms[i]] == \"photophopia\")\n        result = true;\n    }\n}",
        "fixes": ["None"]},
      {"code": "if(num > 3 && result) {{\n    cout << \"You will turn into a walker...\" << endl;\n    return 0;\n}",
        "fixes": ["None"]},
      {"code": "cout << \"You are not infected\" << endl;\nreturn 0;",
        "fixes": ["None"]}
    ]
  }
